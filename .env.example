# KamaClaude 环境配置模板
# S0 新手可以把它理解为“无需改代码就能调整运行参数”的模板。
# 仅当 .env 不存在时复制本模板；已有 .env 可能包含本机密钥，不能直接覆盖。
# PowerShell：if (-not (Test-Path ".env")) { Copy-Item ".env.example" ".env" }
# 真正的 .env 可能包含密钥，已被 .gitignore 忽略，不能提交到 Git。

# ── Core Daemon ───────────────────────────────────────────────
# 127.0.0.1 只允许本机访问；7437 是 kama-core 默认监听端口。
KAMA_HOST=127.0.0.1
KAMA_PORT=7437

# ── Logging ───────────────────────────────────────────────────
KAMA_LOG_LEVEL=INFO
# 留空则仅输出 stderr，不写文件
KAMA_LOG_FILE=.kama/logs/core.log
# text（人工可读键值对）或 json（结构化，适合日志聚合）
KAMA_LOG_FORMAT=text

# ── Config File ───────────────────────────────────────────────
# 取消下一行注释后，可以指定其他 TOML 配置文件。
# KAMA_CONFIG=.kama/config.toml

# ── LLM（S1 阶段启用）────────────────────────────────────────
# S0 完全不调用 LLM，下面三项现在不用配置。
# S1 第一次真正调用模型：API key 负责鉴权，模型名决定调用谁，max_steps 防止循环失控。
# 只复制本模板，不要把真实 API key 写回或提交到 .env.example。
# ANTHROPIC_API_KEY=sk-ant-...
# KAMA_LLM_DEFAULT_MODEL=claude-sonnet-4-6
# KAMA_MAX_STEPS=20
